# 三轴步进电机控制系统 v2.0 - 高内聚低耦合设计

## 📋 项目概述

本项目是一个基于Arduino的三轴步进电机控制系统，采用高内聚、低耦合的模块化设计。系统将功能明确分离：手动调试、ROI视觉处理、串口通讯各自独立，实现更好的可维护性和可扩展性。

### 主要特性
- ✅ 三轴独立控制 (X/Y/Z轴)
- ✅ 手动调试模式 (专用命令处理)
- ✅ ROI视觉数据处理 (独立模块)
- ✅ 串口通讯协议 (清晰的数据流)
- ✅ 高内聚低耦合设计
- ✅ 内存优化 (适配Arduino Uno 2KB RAM)
- ✅ 实时运动控制

## 🏗️ 系统架构 v2.0

### 模块关系图
```
┌─────────────────┐
│    main.cpp     │  ← 主程序入口，系统调度
│   (系统调度)    │
└─────────┬───────┘
          │
          ├─────────────────────────────────────┐
          │                                     │
    ┌─────▼─────┐                         ┌─────▼─────┐
    │   串口    │                         │   ROI     │
    │   通信    │ ← SerialCommunication   │   处理    │ ← ROIProcessor
    │   协议    │   (专注串口功能)        │   模块    │   (视觉数据处理)
    └─────┬─────┘                         └─────┬─────┘
          │                                     │
    ┌─────▼─────┐                               │
    │   命令    │                               │
    │   处理    │ ← CommandHandler              │
    │   模块    │   (手动调试专用)              │
    └─────┬─────┘                               │
          │                                     │
          └─────────────┬───────────────────────┘
                        │
                  ┌─────▼─────┐
                  │   电机    │
                  │   控制    │ ← StepperControl
                  │   模块    │   (底层控制)
                  └───────────┘
```

### 设计原则
- **高内聚**: 每个模块专注单一职责
- **低耦合**: 模块间通过明确接口通信
- **单一职责**: 每个类只负责一个明确的功能
- **依赖注入**: 通过构造函数注入依赖

### 文件结构
```
3AxisControlProject/
├── src/
│   ├── main.cpp                 # 主程序 - 系统初始化和调度
│   ├── stepper_control.cpp      # 电机控制实现 - 底层控制
│   ├── command_handler.cpp      # 手动调试命令处理 - 专注调试功能
│   ├── serial_communication.cpp # 串口通信协议 - 专注通讯功能
│   └── roi_processor.cpp        # ROI视觉数据处理 - 独立处理模块
├── include/
│   ├── config.h                 # 系统配置文件
│   ├── stepper_control.h        # 电机控制接口
│   ├── command_handler.h        # 命令处理接口
│   ├── serial_communication.h   # 串口通信接口
│   └── roi_processor.h          # ROI处理接口
└── platformio.ini               # PlatformIO配置
├── include/
│   ├── config.h                 # 配置文件
│   ├── stepper_control.h        # 电机控制头文件
│   ├── command_handler.h        # 命令处理头文件
│   └── serial_communication.h   # 串口通信头文件
└── platformio.ini              # PlatformIO配置
```

## 🔧 模块详细说明

### 1. 主程序模块 (main.cpp)
**职责**: 系统初始化和调度控制
- 初始化所有子模块
- 协调各模块之间的工作
- 提供系统启动信息

```cpp
// 新的高内聚设计
StepperControl stepperControl;                    // 电机控制
CommandHandler commandHandler(&stepperControl);   // 手动调试
ROIProcessor roiProcessor(&stepperControl);       // ROI处理  
SerialCommunication serialComm(&commandHandler, &roiProcessor); // 串口协议
```

### 2. 串口通讯模块 (SerialCommunication)
**职责**: 专注于串口通讯协议处理
- 数据接收和发送
- 协议解析和分发
- 错误处理和验证

**主要功能**:
- 自动识别ROI数据格式并转发给ROIProcessor
- 识别手动命令并转发给CommandHandler
- 提供统一的通讯接口

### 3. 命令处理模块 (CommandHandler) 
**职责**: 专门处理手动模式的调试命令
- 手动模式切换 (HandMode/AutoMode)
- 调试命令解析和执行
- 测试功能和帮助信息

**手动调试命令**:
- `O` - 获取当前位置
- `V<num>` - 移动到绝对位置
- `M<num>` - 相对移动
- `D<num>` - 选择电机 (0=全部, 1=X, 2=Y, 3=Z)
- `A<num>` - 设置加速度
- `X<num>` - 设置最大速度
- `H` - 显示帮助
- `T` - 测试所有轴

### 4. ROI处理模块 (ROIProcessor)
**职责**: 专门处理上位机传来的ROI视觉数据
- ROI数据解析 (`ROI1,X123.45,Y67.89`)
- 像素坐标到实际坐标转换
- 批量ROI点的运动执行
- ROI数据管理

**工作流程**:
1. 接收ROI数据并解析
2. 累积到指定数量或收到处理命令
3. 转换像素坐标为实际移动距离
4. 按序执行移动：Y轴→X轴→触发动作

### 5. 电机控制模块 (StepperControl)
**职责**: 底层步进电机控制
- 三轴电机的初始化和控制
- 运动指令的执行
- 位置反馈和状态查询

// 轴选择机制
void StepperControl::setCurrentStepper(int num) {
    currentStepperNum = num;  // 1=X轴, 2=Y轴, 3=Z轴
}

// 位置控制
void StepperControl::moveTo(int data) {
    int steps = data * OneMM;  // 转换为步数 (OneMM = 40步/毫米)
    
    switch(currentStepperNum) {
        case 1: stepperX.moveTo(steps); break;
        case 2: stepperY.moveTo(steps); break;
        case 3: stepperZ.moveTo(steps); break;
    }
}
```

### 3. 串口通信模块

#### 双重处理机制
```cpp
class SerialCommunication {
private:
    // 基础通信参数
    CommandHandler* commandHandler;
    HardwareSerial* serialPort;
    unsigned long baudRate;
    bool isInitialized;
    
    // ROI数据处理
    static const int MAX_ROI_COUNT = 10;
    ROIData roiDataArray[MAX_ROI_COUNT];
    size_t roiCount;
    
    // 缓冲区管理
    char inputBuffer[128];
    size_t bufferIndex;
    bool stringComplete;
};
```

#### 数据处理流程
```cpp
void SerialCommunication::processReceivedData() {
    // A. 基础命令处理路径
    if (isDataAvailable()) {
        String command = readCommand();
        commandHandler->processCommand(command);
    }
    
    // B. ROI数据处理路径
    processROIData();
}

// ROI数据处理逻辑
void SerialCommunication::processROIData() {
    // 1. 字符级别数据接收
    while (serialPort->available()) {
        char inChar = (char)serialPort->read();
        
## 🎮 使用指南

### 1. 系统启动
上电后系统会显示启动信息：
```
========================================
  三轴步进电机控制系统 v2.0
  High Cohesion & Low Coupling Design
========================================
System modules initialized:
  ✓ StepperControl
  ✓ CommandHandler (Manual Debug Mode)
  ✓ ROIProcessor (Vision Data Processing)
  ✓ SerialCommunication (Protocol Handler)
========================================
Commands:
  HandMode - Enter manual debug mode
  AutoMode - Enter automatic mode
  Type 'H' in HandMode for help
========================================
```

### 2. 手动调试模式
发送 `HandMode` 进入手动调试模式，然后可以使用以下命令：

#### 基本命令
- `D1` - 选择X轴电机
- `D2` - 选择Y轴电机  
- `D3` - 选择Z轴电机
- `D0` - 选择所有电机
- `O` - 获取当前位置

#### 移动命令
- `V100` - 移动到绝对位置100
- `M50` - 相对移动50步
- `R200` - 立即运行到位置200

#### 参数设置
- `A500` - 设置加速度为500
- `X1000` - 设置最大速度为1000

#### 测试命令
- `T` - 测试所有轴
- `H` - 显示帮助信息

### 3. 自动模式（ROI处理）
发送 `AutoMode` 进入自动模式，然后可以发送ROI数据：

#### ROI数据格式
```
ROI1,X123.45,Y67.89
ROI2,X200.30,Y150.20
ROI3,X300.15,Y250.60
...
```

#### ROI处理命令
- `PROCESS_ROI` - 处理所有已接收的ROI数据
- `CLEAR_ROI` - 清空ROI数据缓存

### 4. 系统响应
- `ACK` - ROI数据接收确认
- `ROI_FULL` - ROI缓存已满
- `ROI_PROCESSED` - ROI处理完成
- `ROI_CLEARED` - ROI数据已清空
│ Pin 2  ──────────► │ X轴 STEP    │
│ Pin 3  ──────────► │ X轴 DIR     │
│ Pin 4  ──────────► │ Y轴 STEP    │
│ Pin 5  ──────────► │ Y轴 DIR     │
│ Pin 6  ──────────► │ Z轴 STEP    │
│ Pin 7  ──────────► │ Z轴 DIR     │
│ GND    ──────────► │ GND         │
│ 5V     ──────────► │ VCC         │
└─────────────┘      └─────────────┘
```

### 软件使用

#### 1. 程序上传
```bash
# 使用PlatformIO
cd /path/to/3AxisControlProject
pio run --target upload

# 或在VS Code中
# Ctrl+Shift+P → "PlatformIO: Upload"
```

#### 2. 串口连接
- **波特率**: 115200
- **数据位**: 8
- **停止位**: 1
- **校验位**: 无

#### 3. 基础命令控制

**轴选择命令：**
```bash
1    # 选择X轴为当前操作轴
2    # 选择Y轴为当前操作轴  
3    # 选择Z轴为当前操作轴
```

**移动命令：**
```bash
X100   # X轴移动到100mm位置
Y50    # Y轴移动到50mm位置
Z25    # Z轴移动到25mm位置
```

**特殊命令：**
```bash
0      # 设置当前位置为零点
```

#### 4. 使用示例

**示例1：基础控制**
```bash
串口发送：1        # 选择X轴
系统响应：(无回复，内部状态改变)

串口发送：X100     # X轴移动到100mm
系统响应：(电机开始运动)

串口发送：2        # 选择Y轴
串口发送：Y50      # Y轴移动到50mm
```

**示例2：ROI数据处理**
```bash
# 发送ROI数据 (格式：ROI编号,X像素坐标,Y像素坐标)
串口发送：ROI1,X320,Y240
串口发送：ROI2,X400,Y300
串口发送：ROI3,X200,Y150
串口发送：ROI4,X500,Y400
串口发送：ROI5,X150,Y100
串口发送：ROI6,X350,Y250
串口发送：ROI7,X450,Y350
串口发送：ROI8,X250,Y200
串口发送：ROI9,X550,Y450
串口发送：ROI10,X300,Y300

# 系统自动处理
系统响应：All ROI data received
系统响应：Moving to ROI 1: X=32mm, Y=24mm
系统响应：Moving to ROI 2: X=40mm, Y=30mm
系统响应：...
系统响应：Coordinate conversion complete
```

## 🏆 设计优势

### 高内聚低耦合的体现

#### 1. 模块职责明确
- **StepperControl**: 专注底层电机控制
- **CommandHandler**: 专注手动调试功能  
- **ROIProcessor**: 专注ROI视觉数据处理
- **SerialCommunication**: 专注串口协议处理

#### 2. 接口清晰简洁
```cpp
// 清晰的依赖注入
CommandHandler(&stepperControl)       // 命令处理依赖电机控制
ROIProcessor(&stepperControl)         // ROI处理依赖电机控制
SerialCommunication(&handler, &roi)   // 串口依赖命令和ROI处理
```

#### 3. 扩展性良好
- 新增ROI处理算法：只需修改ROIProcessor
- 新增调试命令：只需修改CommandHandler  
- 新增通讯协议：只需修改SerialCommunication
- 各模块互不影响

### 与原版本对比

| 特性 | v1.0 (原版本) | v2.0 (重构版本) |
|------|---------------|-----------------|
| 架构设计 | 混合耦合 | 高内聚低耦合 |
| ROI处理 | 混在串口模块中 | 独立ROI处理器 |
| 命令处理 | 混合自动/手动 | 专注手动调试 |
| 代码维护 | 困难 | 容易 |
| 功能扩展 | 复杂 | 简单 |
| 测试验证 | 困难 | 模块化测试 |

## ⚙️ 技术细节

### 内存优化策略
- 使用固定大小数组替代动态String
- 合理控制ROI缓存数量
- 避免不必要的字符串操作
- 使用F()宏存储常量字符串在Flash中

### 实时性保证
- 非阻塞式数据处理
- 电机运动的持续调用
- 最小化主循环延时

### 错误处理机制
- 数据验证和格式检查
- 缓冲区溢出保护
- 命令格式错误提示
- 模块状态检查

## � 总结

本次重构实现了以下目标：

1. **职责分离**: 每个模块专注单一功能
2. **降低耦合**: 通过依赖注入实现模块间通信
3. **提高内聚**: 相关功能聚集在同一模块
4. **增强可维护性**: 代码结构清晰，易于理解和修改
5. **提升可扩展性**: 新功能可以独立开发和测试

系统现在更加健壮、可维护，并且为后续功能扩展奠定了良好的基础。

---

**文档版本**: v2.0  
**最后更新**: 2025年6月29日  
**适用系统**: Arduino Uno + PlatformIO  
**依赖库**: AccelStepper v1.64+  
**设计原则**: 高内聚、低耦合、单一职责