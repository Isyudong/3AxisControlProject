# 三轴步进电机控制系统 v2.1 - 高内聚低耦合设计

## 📋 项目概述

本项目是一个基于Arduino的三轴步进电机控制系统，采用高内聚、低耦合的模块化设计。系统支持两种工作模式：
- **自动批量处理模式**（与上位机严格协议同步，推荐生产环境）
- **手动调试模式**（用于开发和维护调试）

### 主要特性
- ✅ 三轴独立控制 (X/Y/Z轴)
- ✅ 手动调试模式 (专用命令处理)
- ✅ 串口通讯协议（支持上位机批量目标同步）
- ✅ 高内聚低耦合设计
- ✅ 内存优化 (适配Arduino Uno 2KB RAM)
- ✅ 实时运动控制

## 🏗️ 系统架构 v2.1

```
┌─────────────────┐
│    main.cpp     │  ← 主程序入口，系统调度
└─────────┬───────┘
          │
    ┌─────▼─────┐
    │   串口    │
    │   通信    │ ← SerialCommunication
    └─────┬─────┘
          │
    ┌─────▼─────┐
    │   命令    │
    │   处理    │ ← CommandHandler
    └─────┬─────┘
          │
    ┌─────▼─────┐
    │   ROI     │
    │   处理    │ ← ROIProcessor
    └─────┬─────┘
          │
    ┌─────▼─────┐
    │   电机    │
    │   控制    │ ← StepperControl
    └───────────┘
```

### 文件结构
```
3AxisControlProject/
├── src/
│   ├── main.cpp                 # 主程序 - 系统初始化和调度
│   ├── stepper_control.cpp      # 电机控制实现 - 底层控制
│   ├── command_handler.cpp      # 手动调试命令处理 - 专注调试功能
│   ├── serial_communication.cpp # 串口通信协议 - 专注通讯功能
│   └── roi_processor.cpp        # ROI视觉数据处理 - 独立处理模块
├── include/
│   ├── config.h                 # 系统配置文件
│   ├── stepper_control.h        # 电机控制接口
│   ├── command_handler.h        # 命令处理接口
│   ├── serial_communication.h   # 串口通信接口
│   └── roi_processor.h          # ROI处理接口
├── platformio.ini               # PlatformIO配置
├── 项目结构介绍.md              # 本文档
└── ...（测试、文档等）
```

## 🔧 模块说明与协议

### 1. 主程序模块 (main.cpp)
- 系统初始化、模块调度、主循环
- 负责串口数据分发和电机持续运行

### 2. 串口通讯模块 (SerialCommunication)
- **自动模式**：严格遵循上位机协议
    - `BATCH_START,COUNT=N` → `BATCH_READY`
    - `TARGET,SEQ=1,X...,Y...` → 运动完成后 `TARGET_DONE,SEQ=1`
    - `BATCH_COMPLETE,SUCCESS=N,TOTAL=N` → 归零后 `BATCH_FINISHED`
    - 错误应答：`ERROR: ...`
- **手动模式**：转发所有命令到 CommandHandler，支持调试命令
- 自动/手动模式可随时切换

### 3. 命令处理模块 (CommandHandler)
- 仅在手动调试模式下生效
- 支持 `HandMode`/`AutoMode` 切换、单轴/多轴运动、参数设置、帮助等

### 4. ROI处理模块 (ROIProcessor)
- 兼容保留，支持像素到毫米转换、批量ROI点处理
- 新协议下主要用于兼容和扩展

### 5. 电机控制模块 (StepperControl)
- 三轴步进电机底层控制，支持归零、单轴/多轴运动、参数设置

## 🚦 通信协议（自动批量模式）

### 上位机→下位机
- `BATCH_START,COUNT=N`：通知下位机本批次目标数量
- `TARGET,SEQ=1,X...,Y...`：下发单个目标坐标
- `BATCH_COMPLETE,SUCCESS=N,TOTAL=N`：通知批次结束

### 下位机→上位机
- `BATCH_READY`：准备好接收目标
- `TARGET_DONE,SEQ=1`：单个目标处理完成
- `BATCH_FINISHED`：批次归零完成
- `ERROR: ...`：任何异常或格式错误

## 🎮 手动调试模式
- 发送 `HandMode` 进入手动调试模式，所有命令走 CommandHandler
- 发送 `AutoMode` 恢复自动批量协议
- 支持所有原有调试命令（D1/V100/M50/R200/A500/X1000/H/T等）

## 🏆 设计优势
- 高内聚低耦合，易维护、易扩展
- 自动/手动模式随时切换，开发生产两不误
- 严格协议同步，批量处理高可靠
- 兼容原有调试命令，便于开发维护

---

**文档版本**: v2.1  
**最后更新**: 2025年7月3日  
**适用系统**: Arduino Uno + PlatformIO  
**依赖库**: AccelStepper v1.64+  
**设计原则**: 高内聚、低耦合、单一职责