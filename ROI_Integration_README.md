# ROI 数据流处理功能说明

## 概述
采用流处理模式实现 ROI 数据处理功能，支持大量数据（最多2000个ROI）的实时处理，同时保持低内存占用。

## 功能特性

### 1. ROI 数据接收
- 支持接收格式为 `ROI{index},X{x_coord},Y{y_coord}` 的数据
- 例如：`ROI1,X123.45,Y678.90`
- 支持任意数量的 ROI 数据（测试支持最多2000个）
- 上位机每次发送1-10个ROI数据

### 2. 流处理模式
1. **实时处理**：每接收到一个ROI数据立即处理，无需等待批次完成
2. **内存优化**：仅使用10个ROI的缓冲区（120字节），无内存限制
3. **即时应答**：每个ROI处理完成后立即发送确认消息
4. **错误恢复**：单个ROI处理失败不影响后续数据

### 3. 坐标处理
- **直接使用实际坐标**：上位机发送的已经是实际坐标数据，无需转换
- **立即执行**：接收到ROI数据后立即执行电机移动
- **实时反馈**：每个ROI处理状态都有对应的应答

### 4. 应答协议
- **成功应答**：`ACK_ROI{index}` - 例如：`ACK_ROI1`、`ACK_ROI2`
- **失败应答**：`NACK_ROI{index}` - 例如：`NACK_ROI1`、`NACK_ROI2`
- **处理状态**：实时显示已处理的ROI总数
- **兼容性**：保持与原有命令处理系统的兼容性

## 使用方法

### 发送 ROI 数据
```
ROI1,X100.5,Y200.3
ROI2,X150.2,Y180.7
ROI3,X200.1,Y150.9
...
ROI2000,X300.1,Y400.9
```

### 预期响应
```
ACK_ROI1        # 第一个ROI处理成功
ACK_ROI2        # 第二个ROI处理成功
ACK_ROI3        # 第三个ROI处理成功
...
ACK_ROI2000     # 第2000个ROI处理成功
```

### 发送模式
- **单个发送**：发送一个ROI，等待应答，再发送下一个
- **批量发送**：一次发送多个ROI（最多10个），分别接收应答
- **混合发送**：根据网络状况灵活调整发送数量

## 集成说明

### 修改的文件
1. `include/serial_communication.h` - 更新为流处理模式的数据结构和方法声明
2. `src/serial_communication.cpp` - 实现流处理的 ROI 数据处理逻辑

### 新的数据结构
- `ROIData batchBuffer[BATCH_BUFFER_SIZE]` - 批次缓冲区（10个ROI）
- `int currentBatchCount` - 当前批次中的ROI数量
- `int totalProcessedCount` - 总共已处理的ROI数量
- `String inputBuffer` - 串口输入缓冲区
- `bool stringComplete` - 数据接收完成标志

### 核心方法
- `processROIData()` - 主要的流处理方法
- `parseROIString()` - ROI 字符串解析方法
- `processIndividualROI()` - 立即处理单个ROI数据
- `sendACK()` - 发送应答消息给上位机
- `clearBatchBuffer()` - 清空批次缓冲区

## 技术优势

### 内存效率
- **低内存占用**：仅使用120字节缓冲区（10个ROI × 12字节）
- **无数量限制**：可处理任意数量的ROI数据（测试支持2000个）
- **内存安全**：避免Arduino内存溢出问题

### 实时性能
- **流式处理**：接收即处理，无需等待批次完成
- **即时反馈**：每个ROI处理完成立即应答
- **并行处理**：数据接收和电机控制并行执行

### 可靠性
- **错误隔离**：单个ROI失败不影响整体处理
- **状态跟踪**：实时显示处理进度和状态
- **协议健壮**：支持重传和错误恢复

## 注意事项
1. **应答机制**：上位机应等待每个ROI的应答后再发送下一批数据
2. **坐标系统**：上位机发送的坐标已经是实际物理坐标，无需转换
3. **处理速度**：ROI处理速度取决于电机移动时间
4. **错误处理**：建议上位机实现超时重传机制
5. **兼容性**：普通命令处理功能保持不变
